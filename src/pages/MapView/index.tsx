import React from 'react'
import { View, Text } from 'react-native'

import styles from './styles'

const MapView: React.FC = () => {
  return (
    <View style={styles.container}>
      <Text>Template de projeto expo manage</Text>
    </View>
  )
}

export default MapView
